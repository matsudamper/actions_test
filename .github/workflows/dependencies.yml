name: Dependencies
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    if: ${{ github.event.label.name }} == "dependencies"
    uses: ./.github/workflows/call_test.yml
