name: Dependencies
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    if: ${{ github.event.label.name }} == "dependencies"
    runs-on: ubuntu-latest
    steps:
      - uses: matsudamper/actions_test/.github/workflows/call_ktlint@${{ github.sha }}
      - uses: .github/workflows/call_test
