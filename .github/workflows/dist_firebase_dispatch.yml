name: issue comment
on:
  issue_comment:
    types: [ created ]

permissions:
  contents: read

jobs:
  issue_comment:
    if: ${{ github.event.issue.pull_request && github.event.comment.body == './distFirebase' }}
    uses: ./.github/workflows/call/deploy_app_distribution.yml
